<div class="container fade-in" style="max-width: 800px;">
  <h2 class="display-6 fw-bold mb-4 p-3 text-center" style="background: #e0f7fa; color: #009fe8; border-radius: 8px;">タグ一覧</h2>

  <div class="mb-4">
    <p class="text-muted">タグをクリックすると、そのタグが付いた契約書の一覧が表示されます。</p>
  </div>

  <div class="row">
    <% @tags.each do |tag| %>
      <div class="col-md-4 col-sm-6 mb-3">
        <div class="card h-100 shadow-sm">
          <div class="card-body text-center">
            <h5 class="card-title">
              <%= link_to tag.name, contracts_path(tag: tag.name), class: "text-decoration-none" %>
            </h5>
            <p class="card-text text-muted">
              <%= tag.taggings_count %>件の契約書
            </p>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <div class="mt-4 text-center">
    <%= link_to "契約書一覧に戻る", contracts_path, class: "btn btn-secondary px-4" %>
  </div>
</div> 