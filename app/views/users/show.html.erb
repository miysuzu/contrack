<div class="container fade-in" style="max-width: 800px;">
  <h2 class="display-6 fw-bold mb-4 p-3 text-center" style="background: #e0f7fa; color: #009fe8; border-radius: 8px;">マイページ</h2>
  <!-- ユーザー情報 -->
  <div class="mb-4 px-3">
    <p class="mb-2"><strong>氏名：</strong><%= @user.name %></p>
    <p class="mb-2"><strong>メールアドレス：</strong><%= @user.email %></p>
    <div class="mt-3">
      <%= link_to '会員情報を編集する', edit_user_path, class: 'btn btn-outline-primary me-2' %>
    </div>
  </div>
  <!-- 契約書一覧 -->
  <h3 class="mt-4 mb-3">契約書一覧</h3>
  <% if @contracts.any? %>
    <table class="table table-striped align-middle shadow-sm">
      <thead class="table-light">
        <tr>
          <th>タイトル</th>
          <th>ステータス</th>
          <th>作成日</th>
          <th>操作</th>
        </tr>
      </thead>
      <tbody>
        <% @contracts.each do |contract| %>
          <tr>
            <td><%= contract.title %></td>
            <td><%= contract.status.name if contract.status %></td>
            <td><%= l(contract.created_at, format: :short) %></td>
            <td><%= link_to '詳細', contract_path(contract), class: "btn btn-sm btn-outline-primary" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p>契約書はまだ登録されていません。</p>
  <% end %>
</div>
